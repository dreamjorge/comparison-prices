# Vite dev server proxies /api -> http://localhost:8000 by default.
# Override this only if your API is exposed elsewhere.
VITE_API_BASE_URL=/api

# Dev key expected by the API (api/.env.example)
VITE_API_KEY=dummy-dev-key
